{
  "name": "greeting-lib",
  "version": "1.0.0",
  "main": "dist/main.js",
  "license": "MIT",
  "dependencies": {
    "hello-lib": "1.0.0"
  },
  "scripts": {
    "build": "npx esbuild src/main.js --bundle --format=esm --packages=external --outdir=dist"
  }
}
